# O pnpm >=10.16 rejeita pacotes publicados há menos de 24h (minimumReleaseAge).
# O lockfile é commitado e as versões são fixas, então liberamos a checagem para
# builds reproduzíveis no Docker/Dokploy não falharem com deps recém-publicadas.
minimum-release-age=0
